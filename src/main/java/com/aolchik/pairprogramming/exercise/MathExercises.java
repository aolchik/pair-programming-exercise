package com.aolchik.pairprogramming.exercise;

public class MathExercises {
	/*
	 * Implement a method that returns the fibonacci sequence where
	 * the next number is the sum of the previous two.
	 * fib(0) -> 0
	 * fib(1) -> 1
	 * fib(2) -> 1
	 * fib(3) -> 2
	 * fib(4) -> 3
	 * fib(5) -> 5
	 */
	
	public static int fibo(int n) {
		// @TODO: Pending implementation
		return 0;
	}
	
}